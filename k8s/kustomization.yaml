apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vkbot

resources:
  - namespace.yaml
  - postgres-deployment.yaml
  - postgres-service.yaml
  - vkbot-deployment.yaml
  - backup-pvc.yaml
  - backup-cronjob.yaml
